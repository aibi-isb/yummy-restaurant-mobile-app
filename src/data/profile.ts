// ─── Menu icon names (Phosphor) ─────────────────────────────────────────────────

/** Address — home/location icon */
export const iconAddress = "map-pin";

/** Order History — cart/bag icon */
export const iconOrderHistory = "clock-counterclockwise";

/** Account Settings — person/gear icon */
export const iconAccountSettings = "gear";

/** Payment Methods — card icon */
export const iconPaymentMethods = "credit-card";

/** Log Out — exit/logout icon */
export const iconLogOut = "sign-out";

/** Edit pencil icon (inline next to phone / email / section header) */
export const iconEdit = "pencil-simple";

/** Phone edit icon */
export const iconPhone = "pencil-simple";

/** Email icon */
export const iconEmail = "envelope-simple";

// ─── Order History stat icons ─────────────────────────────────────────────────

/** Total Orders circle icon */
export const iconTotalOrders = "receipt";

/** Received Orders circle icon */
export const iconReceivedOrders = "package";

/** Successful Payments circle icon */
export const iconSuccessfulPayments = "check-circle";

// ─── Shared types ─────────────────────────────────────────────────────────────

export type SubItem = { label: string; value: string };
